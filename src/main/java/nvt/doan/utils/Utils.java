package nvt.doan.utils;

import nvt.doan.dto.HomestayDTO;
import nvt.doan.entities.FileData;
import nvt.doan.entities.Room;

import java.util.ArrayList;
import java.util.List;

public class Utils {

//    public static List<Room> mappingRoomList( HomestayDTO form) {
//        List<Room> list = new ArrayList<>();
//        for(int i = 0; i < form.getRoomId().length;i++){
//            Room room = new Room();
//            room.setRoomId(Integer.parseInt(form.getRoomId()[i]));
//            room.setRoomName(form.getRoomName()[i]);
//            room.setRoomDescription(form.getRoomDescription()[i]);
//            room.setArea(Double.parseDouble(form.getArea()[i]));
//            list.add(room);
//        }
//        return list;
//    }
//    public static List<FileData> mappingImagesList

}
